{
  "name": "aibility-slack-reminders",
  "version": "0.1.0",
  "private": true,
  "description": "Slack reminders for Aibility webinars",
  "main": "api/cron.ts",
  "scripts": {
    "dev": "node -e \"console.log('Use Vercel dev or deploy to run cron.')\"",
    "build": "node -e \"console.log('No build step required.')\""
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "redis": "^5.10.0"
  },
  "engines": {
    "node": "24.x"
  }
}
